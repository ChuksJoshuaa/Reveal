interface RelatedProps {
  userId?: string | null;
  projectId?: string | null;
}

const RelatedProjects = ({ userId, projectId }: RelatedProps) => {
  return <div>RelatedProjects</div>;
};

export default RelatedProjects;
